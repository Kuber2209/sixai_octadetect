fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.5

# Production database driver (the dev fallback is SQLite, no driver needed):
# psycopg2-binary>=2.9

# ThingMagic Mercury API Python bindings are NOT on PyPI — build from source
# on the server (see README "Reader setup"):
#   https://github.com/gotthardp/python-mercuryapi
